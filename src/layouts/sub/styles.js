import colors from "../../utils/colors/colors";

const styles = {
    navbar: { backgroundColor: colors.lightBlue },
    navbarButton: { marginLeft: 20 },
    navbarBrand: { marginLeft: 20 }
}

export default styles;